micronaut:
  application:
    name: users-service
  server:
    port: 8002
    cors:
      enabled: true
      configurations:
        web:
          allowedOrigins:
            - http://localhost:3000
            - http://localhost:3001
            - http://localhost:5173
          allowedMethods:
            - GET
            - POST
            - PUT
            - DELETE
            - OPTIONS
          allowedHeaders:
            - Content-Type

logger:
  levels:
    root: INFO
    com.users: INFO

datasources:
  default:
    url: jdbc:sqlite:./data/users.sqlite3
    driverClassName: org.sqlite.JDBC
    maximumPoolSize: 5
