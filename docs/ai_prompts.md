# AI Prompts

Suggested prompts for future work:

- "Add PATCH endpoints to update bookings estado and persist changes"
- "Add pagination to GET /api/tours and /api/users"
- "Add integration tests for bookings-service validating tourId/userId"
- "Add Dockerfiles + docker-compose to run all services"
